name: flutter_application_3
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.2
  logger: ^1.0.0
  path_provider: ^2.0.6
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  firebase_core: ^2.24.2 # Firebase Core
  firebase_auth: ^4.15.3 # Autenticación de Firebase
  firebase_database: ^10.3.8 # Base de datos en tiempo real de Firebase

flutter:
  uses-material-design: true
  assets:
    - assets/db.json
    - .env
